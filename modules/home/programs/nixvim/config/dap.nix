{
  plugins.dap = {
    enable = true;
  };

  plugins.dap-go = {
    enable = true;
  };

  plugins.dap-ui = {
    enable = true;
    settings = {
      expand_lines = true;
      force_buffers = true;
    };
  };

  plugins.dap-virtual-text = {
    enable = true;
  };
}
