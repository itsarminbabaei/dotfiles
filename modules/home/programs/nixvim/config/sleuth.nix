# Sleuth - Indent detection
# Docs: https://github.com/tpope/vim-sleuth
# Automatically detects indentation style from file content (spaces/tabs/width).

{
  plugins.sleuth = {
    enable = true;
  };
}
