# Persistence - Session management
# Docs: https://github.com/folke/persistence.nvim
# Saves/restores editor state (buffers, folds, cursor) across sessions.

{
  plugins.persistence = {
    enable = true;
    settings = {};
  };
}
