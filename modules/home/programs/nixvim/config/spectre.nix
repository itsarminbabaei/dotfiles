# Spectre - Find and replace
# Docs: https://github.com/nvim-pack/nvim-spectre
# Modern find/replace UI with live preview. Better than built-in :substitute.

{
  plugins.spectre = {
    enable = true;
    settings = {};
  };
}
